# FlexPortal Dashboard

This project is part of the **Web Development Course (HW2)** at Tel-Hai College.  
It demonstrates advanced **Flexbox layouts**, responsive design, and dynamic navigation using **HTML, CSS, and JavaScript**.

---

## 📄 Project Structure
- `index.html` — Main entry page with iframe navigation.
- `dashboard.html` — Flex-based card grid layout.
- `styles.css` — Base theme, colors, and page structure.
- `flex.css` — Flexbox behavior for layout and card responsiveness.
- `script.js` — Dynamic navigation and theme toggle (dark/light mode).
- Additional pages: `library.html`, `player.html`, `profile.html`, `about.html`.

---

## 💡 Features
- Responsive layout using **Flexbox** (no media queries required).
- Fixed header and footer with interactive navigation bar.
- Dynamic page loading via `<iframe>`.
- **Theme toggle** (Dark / Light mode).
- Clean, modern UI with reusable CSS variables.

---

## 🚀 How to Run
1. Clone or download the repository.  
2. Open `index.html` in your browser.  
3. Use the navigation bar to switch between pages inside the iframe.

---

## 🧑‍💻 Author
**Aviad Benita**  
Tel-Hai College — Web Development Course
